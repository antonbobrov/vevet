---
sidebar_position: 2
description: Swipe methods — API reference. Vevet.js Swipe component.
keywords:
  - swipe methods
  - vevet swipe
---

import Head from '@docusaurus/Head';

# Methods

<Head>
  <title>Swipe Methods | vevet.js</title>
</Head>

:::note
All **[Module methods](/docs/base/Module/#methods)** are available in this class.
:::

### `cancelInertia`

Stops the inertia animation.

```ts
const swipe = new Swipe({
  container: document.getElementById('container'),
});

swipe.cancelInertia();
```

### `cancelBounce`

Stops the bounce-back timeline (out-of-bounds snap when inertia is off).

```ts
const swipe = new Swipe({
  container: document.getElementById('container'),
});

swipe.cancelBounce();
```

### `destroy`

Destroys the instance and cleans up resources.

```ts
const swipe = new Swipe({
  container: document.getElementById('container'),
});

swipe.destroy();
```
