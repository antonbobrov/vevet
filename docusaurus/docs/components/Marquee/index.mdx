---
toc_min_heading_level: 2
toc_max_heading_level: 6
---

# Marquee

A custom marquee component that smoothly scrolls its child elements.

This component is designed to loop elements horizontally within a container, with support for customization such as speed, gap, pause on hover, and more.

## Examples

Explore live examples on CodePen:

### Default Marquee

<iframe 
  height="500"
  style={{
    width: '100%'
  }} 
  scrolling="no" 
  title="Vevet Example"
  src="https://codepen.io/anton-bobrov/embed/zxxOadw?default-tab=result"
  frameborder="no"
  loading="lazy"
  allowtransparency="true"
  allowfullscreen="true"
>
  See the Pen <a href="https://codepen.io/anton-bobrov/pen/zxxOadw">Vevet Example</a> by Anton Bobrov.
</iframe>

### Reverse Marquee

<iframe 
  height="500"
  style={{
    width: '100%'
  }} 
  scrolling="no" 
  title="Vevet Example"
  src="https://codepen.io/anton-bobrov/embed/EaaLbOp?default-tab=result"
  frameborder="no"
  loading="lazy"
  allowtransparency="true"
  allowfullscreen="true"
>
  See the Pen <a href="https://codepen.io/anton-bobrov/pen/EaaLbOp">Vevet Example</a> by Anton Bobrov.
</iframe>

### Draggable Marquee

<iframe 
  height="600"
  style={{
    width: '100%'
  }} 
  scrolling="no" 
  title="Vevet Example"
  src="https://codepen.io/anton-bobrov/embed/XJJrYeR?default-tab=result"
  frameborder="no"
  loading="lazy"
  allowtransparency="true"
  allowfullscreen="true"
>
  See the Pen <a href="https://codepen.io/anton-bobrov/pen/XJJrYeR">Vevet Example</a> by Anton Bobrov.
</iframe>

### Scrollable Marquee

<iframe 
  height="600"
  style={{
    width: '100%'
  }} 
  scrolling="no" 
  title="Vevet Example"
  src="https://codepen.io/anton-bobrov/embed/raaBKYy?default-tab=result"
  frameborder="no"
  loading="lazy"
  allowtransparency="true"
  allowfullscreen="true"
>
  See the Pen <a href="https://codepen.io/anton-bobrov/pen/raaBKYy">Vevet Example</a> by Anton Bobrov.
</iframe>



## Props

### Static Props

Static properties are set during initialization and cannot be modified later.

#### `container` {#props.container}
- **Type:** `HTMLElement`  
- The container element that holds the marquee content.

#### `resizeDebounce` {#props.resizeDebounce}
- **Type:** `number`  
- **Default:** `0`  
- The debounce delay for the resize event in milliseconds.

#### `hasWillChange` {#props.hasWillChange}
- **Type:** `boolean`  
- **Default:** `true`  
- Determines whether to apply the `will-change` CSS property to the marquee elements to optimize rendering. Setting this to `true` may improve performance.

#### `cloneNodes` {#props.cloneNodes}
- **Type:** `boolean`  
- **Default:** `true`  
- Indicates whether to clone the marquee nodes. Can be set to `false` if DOM cloning is not preferred.

### Mutable Props

Mutable properties can be updated at runtime using `.updateProps()`.

#### `speed` {#props.speed}
- **Type:** `number | string`  
- **Default:** `1`  
- The speed of the marquee animation. Supports CSS units like `px`, `rem`, `vw`, `vh`, `svh`.

#### `gap` {#props.gap}
- **Type:** `number | string`  
- **Default:** `0`  
- The gap between the marquee elements. Supports CSS units like `px`, `rem`, `vw`, `vh`, `svh`.

#### `enabled` {#props.enabled}
- **Type:** `boolean`  
- **Default:** `true`  
- Enables or disables the marquee animation. When `false`, the marquee will be paused.

#### `pauseOnHover` {#props.pauseOnHover}
- **Type:** `boolean`  
- **Default:** `false`  
- Pauses the marquee when the mouse hovers over it.

#### `centered` {#props.centered}
- **Type:** `boolean`  
- **Default:** `false`  
- Centers the marquee content within the container.

#### `adjustSpeed` {#props.adjustSpeed}
- **Type:** `boolean`  
- **Default:** `true`  
- When needed, uses dynamic FPS factor to adjust the speed of the marquee.

#### `pauseOnOut` {#props.pauseOnOut}
- **Type:** `boolean`  
- **Default:** `true`  
- Pauses the marquee when the mouse leaves the viewport.



## Accessors

:::note
All **[Module's accessors](/docs/base/Module/#accessors)** are available in this class.
:::

### totalWidth

Type: `number`

Total width of all elements in the marquee

### x

Type: `number`

The current X coordinate of the marquee.



## Methods

:::note
All **[Module's methods](/docs/base/Module/#methods)** are available in this class.
:::

### render

Renders the marquee, adjusting element positions.

```ts
instance.render(2); // with step 2
instance.render(-1); // with step -1
```

### resize

Resizes the marquee, recalculating element positions and cloning if necessary.

```ts
instance.resize();
```

### updateProps

Dynamically updates instance properties.

```ts
instance.updateProps({
  enabled: false,
});
```

### destroy

Destroys the instance and cleans up resources.

```ts
instance.destroy();
```



## Callbacks

:::note
All **[Module's callbacks](/docs/base/Module/#callbacks)** are available in this class.
:::

### render

Called during marquee rendering.

```ts
const destruct = instance.on('render', () => console.log('render'));

// Cancel the callback
destruct();
```

### resize

Called on marquee resize

```ts
const destruct = instance.on('resize', () => console.log('resize'));

// Cancel the callback
destruct();
```