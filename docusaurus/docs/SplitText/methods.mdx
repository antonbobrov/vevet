---
sidebar_position: 2
description: SplitText methods — split, destroy. Vevet.js SplitText API reference.
keywords:
  - vevet.splittext methods
  - vevetjs split text methods
  - vevet split text methods
  - split text methods
---

import Head from '@docusaurus/Head';

# Methods

<Head>
  <title>SplitText Methods | vevet.js</title>
</Head>

:::note
All **[Module methods](/docs/base/Module/#methods)** are available in this class.
:::

### `split`

Splits the text into letters, words, and optionally lines based on configuration.

The method is automatically called on initialization, though it can be manually called at any time.

```ts
const text = new SplitText({ container });

text.split();
```

### `destroy`

Destroys the instance, removes listeners, unwraps lines when possible, and restores the original container DOM.

```ts
const text = new SplitText({ container });

text.destroy();
```
