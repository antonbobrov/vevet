/** 2D coordinates in pointer space (container-relative or viewport, see `relative` prop). */
export interface IPointersVec2 {
  /** Horizontal coordinate (px). */
  x: number;

  /** Vertical coordinate (px). */
  y: number;
}
