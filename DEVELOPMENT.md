# Dev Info

- node v20.17.0
- npm v10.8.2

## Tasks:

- start storybook `npm run storybook`
- start docusaurus `cd docusaurus` & `npm run start`
- release: `npx release-it`

## Check changelog

```
npx release-it --changelog --no-hooks.before:init --no-npm --no-github --no-git.requireBranch --no-git.requireCleanWorkingDir
```
