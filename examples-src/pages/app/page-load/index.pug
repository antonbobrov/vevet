extends ../../_layout/page

block variables
    - var title = 'PageLoad Callbacks';
    - var pageName = 'app/page-load';

block content

    h1 PageLoad callbacks

    p.lead
        a(href="https://antonbobrov.github.io/vevet/docs/classes/PageLoad.html") See the doc 
    
    //- description
    p.lead
        | Callbacks, that are triggered when the page is loaded. 
    p Notice that the HTML and BODY elements may contain the class 
        b "v-loading" 
        | which will be removed once the page is loaded.

    hr
    h2 Example
    include ./sample.pug
    pre.ts
        code!= readFile('./sample.ts', dir)