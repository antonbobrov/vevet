extends ../../_layout/page

block variables
    - var title = 'Preloader';
    - var pageName = 'components/preloader';

block content

    h1 Preloader

    p.lead
        a(href="https://antonbobrov.github.io/vevet/docs/classes/Preloader.html") See the doc
    
    //- description
    p.lead
        b Preloader 
        | is a splash container that is used to hide the main contents until the page is fully loaded.
    p Reload this page and notice the black container above the main contents which is hidden 
        | once the pagу is loaded.

    hr
    include ./sample.pug
    pre.pug
        code!= readFile('./sample.pug', dir)
    pre.ts
        code!= readFile('./sample.ts', dir)
