extends ../../_layout/page

block variables
    - var title = 'CustomCursor';
    - var pageName = 'components/custom-cursor';

block content

    h1 Custom smooth cursor 

    p.lead
        a(href="https://antonbobrov.github.io/vevet/docs/classes/CustomCursor.html") See the doc

    hr
    h2 Example
    include ./sample.pug
    hr
    pre.pug
        code!= readFile('./sample.pug', dir)
    pre.css
        code!= readFile('./sample.css', dir)
    pre.ts
        code!= readFile('./sample.ts', dir)
