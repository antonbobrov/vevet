extends ../../../_layout/page

block variables
    - var title = 'Example of nested timelines';
    - var pageName = 'components-nested-timeline';

block content
    
    .container

        h1 Nested timelined

        hr
        h2 Example
        include ./sample.pug
        hr
        pre.pug
            code!= readFile('./sample.pug', dir)
        pre.css
            code!= readFile('./sample.css', dir)
        pre.ts
            code!= readFile('./sample.ts', dir)
